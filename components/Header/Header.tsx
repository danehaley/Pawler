import { NextPage } from "next";
import Image from 'next/image';

const Header: NextPage = () => {
    return(
        <div>
            <h1 className="text-3xl font-bold underline">Pawler</h1>
        </div>
    );
}

export default Header